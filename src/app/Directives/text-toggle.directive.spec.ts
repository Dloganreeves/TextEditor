import { TextToggleDirective } from './text-toggle.directive';

describe('TextToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new TextToggleDirective();
    expect(directive).toBeTruthy();
  });
});
